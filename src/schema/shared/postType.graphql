enum PostStatus {
  PUBLISHED,
  DRAFT,
  ARCHIVED,
}

type Post {
  id: ID!
  image: String
  title: String!
  description: String!
  content: String
  status: PostStatus
  slug: String!
  author: User!
  category: PostCategory!
  createdAt: DateTime!
  updatedAt: DateTime!
}
